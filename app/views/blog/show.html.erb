<% set_meta_tags :title => @post.title, 
                 :og => { :image => "http://www.nikijazmin.com#{image_path('icon.jpg')}" }
%>

<div class="card">
  <%= render "shared/menu"  %>
</div>

<%= render :partial => 'post', :object => @post %>

